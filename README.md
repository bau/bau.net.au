# bau.net.au

Our site. You will need to jekyll and RedCloth gems, and pygments

    gem install jekyll RedCloth
    pip install pygments

To run locally for dev

    jekyll --server --auto --pygments --safe
